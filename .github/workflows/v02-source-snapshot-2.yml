name: v02 source snapshot 2

on:
  push:
    branches:
      - "ops/v02-source-snapshot-2"
    paths:
      - ".github/workflows/v02-source-snapshot-2.yml"

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: ttf-v02-source-final
          path: |
            pyproject.toml
            src/
            scripts/
            docs/supporting/genetic_conditional_order_contrast_qualification_rule_v0.2.json
          if-no-files-found: error
          retention-days: 1
